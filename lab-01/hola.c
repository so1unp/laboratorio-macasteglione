#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    // Agregar código aquí.
    printf("%s\n", argv[1]);
    // Termina la ejecución del programa.
    exit(EXIT_SUCCESS);
}
